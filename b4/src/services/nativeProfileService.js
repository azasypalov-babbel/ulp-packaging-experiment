// Service created for use for smart-surfaces functionality

// eslint-disable-next-line no-unused-vars
const fetchProfile = (locale) => {
  throw new Error('fetchProfile not implemented for native');
};

export default {
  fetchProfile
};
